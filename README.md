# fleet-manager
Fleet Manager project have two main functionalities:

* Vehicles Management
* Travel Cost Forecast

## Use
To run use `./mvnw spring-boot:run`

Alternatively use of the API is through Swagger `http://localhost:8080/swagger-ui.html` 